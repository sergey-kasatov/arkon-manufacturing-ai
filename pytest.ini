# The suite lives in tests/ and needs no plugins, no fixtures directory and no
# package layout: conftest.py puts the four source directories on the path so the
# scripts under test stay scripts. Kept as a file rather than as pyproject.toml
# because this project ships no package and has nothing else to configure.
[pytest]
testpaths = tests
addopts = -q --strict-markers
