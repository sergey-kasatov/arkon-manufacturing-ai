{
  "id": "arkonTransit01",
  "name": "Arkon Incident Transition v1",
  "nodes": [
    {
      "id": "c1000000-0000-4000-8000-000000000001",
      "name": "Transition Intake",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [
        -1552,
        240
      ],
      "webhookId": "arkon-incident-transition-intake",
      "parameters": {
        "httpMethod": "POST",
        "path": "arkon-incident-transition",
        "responseMode": "responseNode",
        "options": {}
      }
    },
    {
      "id": "c1000000-0000-4000-8000-000000000002",
      "name": "Validate Transition",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -1328,
        240
      ],
      "parameters": {
        "jsCode": "// Arkon Incident Transition - request validation.\n// A transition is a write, so the contract is checked before any store is opened:\n// a malformed request must not consume a transition id.\nconst LIFECYCLE = [\"new\", \"acknowledged\", \"in_containment\", \"resolved\", \"closed\", \"false_positive\"];\nconst OPEN_STATUSES = [\"new\", \"acknowledged\", \"in_containment\"];\nconst TERMINAL_STATUSES = [\"closed\", \"false_positive\"];\nconst REQUESTABLE_STATUSES = [\"acknowledged\", \"in_containment\", \"resolved\", \"closed\", \"false_positive\"];\nconst ALLOWED_TRANSITIONS = {\"new\": [\"acknowledged\", \"false_positive\"], \"acknowledged\": [\"in_containment\", \"resolved\", \"false_positive\"], \"in_containment\": [\"resolved\", \"false_positive\"], \"resolved\": [\"closed\", \"in_containment\", \"false_positive\"], \"closed\": [], \"false_positive\": []};\nconst ACK_WINDOW_MINUTES = {\"P1\": 15, \"P2\": 60};\nconst MAX_NOTE = 500;\n\n// Parameters may arrive in the JSON body or in the query string, for the same\n// reason the escalation endpoint accepts both: Langflow's API Request component\n// can hand a model the URL and nothing else, so an agent-composed body is not\n// possible. The method stays POST, set on the canvas rather than by the model,\n// so the verb still says that this call changes something.\nconst incoming = $input.first().json;\nconst body = { ...(incoming.query ?? {}), ...(incoming.body ?? {}) };\nconst errors = [];\n\nconst text = (value) => (value === undefined || value === null ? \"\" : String(value).trim());\n\nlet incidentId = text(body.incident_id);\nif (!incidentId) {\n  errors.push(\"incident_id is required\");\n} else {\n  const digits = incidentId.replace(/[^0-9]/g, \"\");\n  if (!/^(ARK-INC-)?[0-9]+$/i.test(incidentId) || digits === \"\") {\n    errors.push(\"incident_id must look like ARK-INC-00014 or be a plain number\");\n  } else {\n    incidentId = \"ARK-INC-\" + digits.padStart(5, \"0\");\n  }\n}\n\n// to_status names the state being entered. \"new\" gets its own reason rather than\n// the generic list: it is the intake state, written once by the Steering Cell,\n// and a caller asking for it has misread the endpoint rather than mistyped.\nlet toStatus = text(body.to_status).toLowerCase();\nif (!toStatus) {\n  errors.push(\"to_status is required and must be one of \" + REQUESTABLE_STATUSES.join(\", \"));\n} else if (toStatus === \"new\") {\n  errors.push(\"to_status new is the intake state, written by the Steering Cell and never by a transition\");\n  toStatus = \"\";\n} else if (!REQUESTABLE_STATUSES.includes(toStatus)) {\n  errors.push(\"to_status must be one of \" + REQUESTABLE_STATUSES.join(\", \") + \"; got \" + toStatus);\n  toStatus = \"\";\n}\n\nconst actor = text(body.actor) || \"human operator\";\nconst note = text(body.note);\nif (note.length > MAX_NOTE) {\n  errors.push(\"note must be at most \" + MAX_NOTE + \" characters\");\n}\n\n// The same test affordance, the same three spellings, as the status API and the\n// escalation record. Read after validation and before anything is opened, so a\n// malformed request still gets its 400 and no transition id is consumed. A\n// production deployment removes it or puts it behind an operator role.\nconst simulateFailure = [\"1\", \"true\", \"yes\"].includes(text(body.simulate_failure).toLowerCase());\n\nreturn [\n  {\n    json: {\n      valid: errors.length === 0,\n      errors,\n      simulate_failure: simulateFailure,\n      request: {\n        incident_id: incidentId,\n        to_status: toStatus,\n        actor: actor.slice(0, 120),\n        note: note.slice(0, MAX_NOTE),\n      },\n    },\n  },\n];\n"
      }
    },
    {
      "id": "c1000000-0000-4000-8000-000000000003",
      "name": "Valid?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        -1104,
        240
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 1
          },
          "conditions": [
            {
              "id": "c1000000-0000-4000-8000-000000000003",
              "leftValue": "={{ $json.valid }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      }
    },
    {
      "id": "c1000000-0000-4000-8000-000000000004",
      "name": "Respond Invalid",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        -880,
        416
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({status: \"rejected\", errors: $json.errors}) }}",
        "options": {
          "responseCode": 400
        }
      }
    },
    {
      "id": "c1000000-0000-4000-8000-000000000005",
      "name": "Simulated Failure?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        -880,
        160
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 1
          },
          "conditions": [
            {
              "id": "c1000000-0000-4000-8000-000000000005",
              "leftValue": "={{ $json.simulate_failure }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      }
    },
    {
      "id": "c1000000-0000-4000-8000-000000000006",
      "name": "Respond Simulated Unavailable",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        -656,
        -160
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({status: \"unavailable\", message: \"The transition was not recorded (simulated failure requested by the caller).\"}) }}",
        "options": {
          "responseCode": 503
        }
      }
    },
    {
      "id": "c1000000-0000-4000-8000-000000000007",
      "name": "Read Incident Store",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1.1,
      "position": [
        -656,
        160
      ],
      "onError": "continueErrorOutput",
      "parameters": {
        "operation": "read",
        "fileSelector": "/data/arkon/incidents.jsonl",
        "options": {}
      }
    },
    {
      "id": "c1000000-0000-4000-8000-000000000008",
      "name": "Respond Store Unavailable",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        -432,
        -16
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({status: \"unavailable\", message: \"The incident store could not be read, so the transition was not recorded.\"}) }}",
        "options": {
          "responseCode": 503
        }
      }
    },
    {
      "id": "c1000000-0000-4000-8000-000000000009",
      "name": "Extract Store Text",
      "type": "n8n-nodes-base.extractFromFile",
      "typeVersion": 1.1,
      "position": [
        -432,
        224
      ],
      "parameters": {
        "operation": "text",
        "binaryPropertyName": "data",
        "destinationKey": "store_text",
        "options": {}
      }
    },
    {
      "id": "c1000000-0000-4000-8000-00000000000a",
      "name": "Read Transition Store",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1.1,
      "position": [
        -208,
        224
      ],
      "onError": "continueErrorOutput",
      "parameters": {
        "operation": "read",
        "fileSelector": "/data/arkon/incident_transitions.jsonl",
        "options": {}
      }
    },
    {
      "id": "c1000000-0000-4000-8000-00000000000b",
      "name": "Respond Transition Log Unavailable",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        16,
        32
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({status: \"unavailable\", message: \"The transition log could not be read, so the incident's current status is unknown and nothing was recorded.\"}) }}",
        "options": {
          "responseCode": 503
        }
      }
    },
    {
      "id": "c1000000-0000-4000-8000-00000000000c",
      "name": "Extract Transitions Text",
      "type": "n8n-nodes-base.extractFromFile",
      "typeVersion": 1.1,
      "position": [
        16,
        288
      ],
      "parameters": {
        "operation": "text",
        "binaryPropertyName": "data",
        "destinationKey": "transitions_text",
        "options": {}
      }
    },
    {
      "id": "c1000000-0000-4000-8000-00000000000d",
      "name": "Build Transition Record",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        240,
        288
      ],
      "parameters": {
        "jsCode": "// Arkon Incident Transition - fold the log onto the incident, then decide.\n// Two refusals live here and neither writes anything: an incident that is not in\n// the store, and a transition the lifecycle does not allow from where the\n// incident actually is. The second one needs the fold, because the incident line\n// says \"new\" for the whole life of the incident.\nconst LIFECYCLE = [\"new\", \"acknowledged\", \"in_containment\", \"resolved\", \"closed\", \"false_positive\"];\nconst OPEN_STATUSES = [\"new\", \"acknowledged\", \"in_containment\"];\nconst TERMINAL_STATUSES = [\"closed\", \"false_positive\"];\nconst REQUESTABLE_STATUSES = [\"acknowledged\", \"in_containment\", \"resolved\", \"closed\", \"false_positive\"];\nconst ALLOWED_TRANSITIONS = {\"new\": [\"acknowledged\", \"false_positive\"], \"acknowledged\": [\"in_containment\", \"resolved\", \"false_positive\"], \"in_containment\": [\"resolved\", \"false_positive\"], \"resolved\": [\"closed\", \"in_containment\", \"false_positive\"], \"closed\": [], \"false_positive\": []};\nconst ACK_WINDOW_MINUTES = {\"P1\": 15, \"P2\": 60};\n// Apply the transition log to one incident. The incident line in the store is\n// written once, at new, and never rewritten; the current status is therefore the\n// last transition recorded against it, and the KPI timestamps are the first time\n// each milestone was reached. Charter 7.2.\nfunction arkonFold(incident, transitionsById) {\n  const list = (transitionsById[incident.incident_id] ?? [])\n    .slice()\n    .sort((a, b) => String(a.recorded_at ?? \"\").localeCompare(String(b.recorded_at ?? \"\")));\n\n  const stored = String(incident.status ?? \"new\").toLowerCase();\n  const current = list.length ? String(list[list.length - 1].to_status).toLowerCase() : stored;\n\n  const firstAt = (status) => {\n    const hit = list.find((t) => String(t.to_status).toLowerCase() === status);\n    return hit ? hit.recorded_at : null;\n  };\n\n  const created = Date.parse(incident.created_at ?? \"\");\n  const minutesFromCreated = (iso) => {\n    if (!iso || Number.isNaN(created)) return null;\n    const at = Date.parse(iso);\n    return Number.isNaN(at) ? null : Math.round(((at - created) / 60000) * 10) / 10;\n  };\n\n  const acknowledgedAt = firstAt(\"acknowledged\");\n  const resolvedAt = firstAt(\"resolved\");\n  // A false_positive is a closing outcome: the incident is finished with, and a\n  // response time that counted only \"closed\" would silently drop those cases.\n  const closedAt = firstAt(\"closed\") ?? firstAt(\"false_positive\");\n\n  return {\n    status: current,\n    stored_status: stored,\n    transition_count: list.length,\n    is_terminal: TERMINAL_STATUSES.includes(current),\n    acknowledged_at: acknowledgedAt,\n    resolved_at: resolvedAt,\n    closed_at: closedAt,\n    minutes_to_acknowledge: minutesFromCreated(acknowledgedAt),\n    minutes_to_resolve: minutesFromCreated(resolvedAt),\n    minutes_to_close: minutesFromCreated(closedAt),\n    history: list.map((t) => ({\n      transition_id: t.transition_id ?? null,\n      recorded_at: t.recorded_at ?? null,\n      from_status: t.from_status ?? null,\n      to_status: t.to_status ?? null,\n      actor: t.actor ?? null,\n      note: t.note ?? null,\n    })),\n  };\n}\n\n// Parse a JSONL store into transitions grouped by incident id. Damaged lines are\n// counted rather than thrown: one bad line must not take the whole lifecycle out.\nfunction arkonTransitionsById(text) {\n  const byId = {};\n  let unreadable = 0;\n  for (const line of String(text ?? \"\").split(\"\\n\").map((l) => l.trim()).filter(Boolean)) {\n    try {\n      const record = JSON.parse(line);\n      const id = String(record.incident_id ?? \"\");\n      if (!id) {\n        unreadable += 1;\n        continue;\n      }\n      (byId[id] = byId[id] ?? []).push(record);\n    } catch (error) {\n      unreadable += 1;\n    }\n  }\n  return { byId, unreadable };\n}\n\nconst request = $(\"Validate Transition\").first().json.request;\nconst storeText = $(\"Extract Store Text\").first().json.store_text ?? \"\";\nconst transitionsText = $(\"Extract Transitions Text\").first().json.transitions_text ?? \"\";\n\nconst incidents = [];\nfor (const line of storeText.split(\"\\n\").map((l) => l.trim()).filter(Boolean)) {\n  try {\n    incidents.push(JSON.parse(line));\n  } catch (error) {\n    // A damaged line is not a transition failure; the status API reports the count.\n  }\n}\n\nconst incident = incidents.find((item) => item.incident_id === request.incident_id) ?? null;\nif (!incident) {\n  return [{ json: { found: false, allowed: false, incident_id: request.incident_id } }];\n}\n\nconst folded = arkonFold(incident, arkonTransitionsById(transitionsText).byId);\nconst from = folded.status;\nconst allowedNext = ALLOWED_TRANSITIONS[from] ?? [];\n\nif (!allowedNext.includes(request.to_status)) {\n  return [\n    {\n      json: {\n        found: true,\n        allowed: false,\n        incident_id: incident.incident_id,\n        current_status: from,\n        requested_status: request.to_status,\n        allowed_next: allowedNext,\n        reason: allowedNext.length\n          ? incident.incident_id + \" is \" + from + \", and from there the lifecycle allows only \" + allowedNext.join(\", \")\n          : incident.incident_id + \" is \" + from + \", which is a terminal state; no further transition is possible\",\n      },\n    },\n  ];\n}\n\n// Transition ids come from workflow static data, the same mechanism the intake\n// and escalation workflows use for their ids, and one is consumed only once both\n// refusals above are past. Static data persists for production executions of a\n// published workflow, not for editor test runs.\nconst state = $getWorkflowStaticData(\"global\");\nstate.nextTransitionId = (state.nextTransitionId ?? 0) + 1;\nconst transitionId = \"ARK-TRN-\" + String(state.nextTransitionId).padStart(5, \"0\");\n\nconst recordedAt = new Date();\nconst minutesBetween = (fromMs, toMs) =>\n  Number.isNaN(fromMs) || Number.isNaN(toMs) ? null : Math.round(((toMs - fromMs) / 60000) * 10) / 10;\n\nconst minutesSinceCreated = minutesBetween(Date.parse(incident.created_at ?? \"\"), recordedAt.getTime());\nconst previous = folded.history.length ? folded.history[folded.history.length - 1] : null;\nconst minutesSincePrevious = previous\n  ? minutesBetween(Date.parse(previous.recorded_at ?? \"\"), recordedAt.getTime())\n  : null;\n\n// The acknowledgement window of charter 7.1, evaluated only on the transition it\n// describes and null on every other one. The timestamps are the record of truth;\n// these two numbers are derived at write time so a consumer reading this file\n// alone, a Tableau extract for instance, still has the KPI without the fold.\nconst ackWindow = ACK_WINDOW_MINUTES[String(incident.priority ?? \"\").toUpperCase()] ?? null;\nconst acknowledgedWithinWindow =\n  request.to_status === \"acknowledged\" && ackWindow !== null && minutesSinceCreated !== null\n    ? minutesSinceCreated <= ackWindow\n    : null;\n\nconst record = {\n  transition_id: transitionId,\n  recorded_at: recordedAt.toISOString(),\n  incident_id: incident.incident_id,\n  incident_priority: incident.priority ?? null,\n  incident_created_at: incident.created_at ?? null,\n  from_status: from,\n  to_status: request.to_status,\n  actor: request.actor,\n  note: request.note,\n  minutes_since_created: minutesSinceCreated,\n  minutes_since_previous_transition: minutesSincePrevious,\n  acknowledge_window_minutes: ackWindow,\n  acknowledged_within_window: acknowledgedWithinWindow,\n  // The actor is a simulated role holder, the same boundary the escalation\n  // record draws. The timestamps and the state machine are real.\n  context_origin: \"simulated\",\n};\n\nreturn [\n  {\n    json: {\n      found: true,\n      allowed: true,\n      transition: record,\n      transition_jsonl: JSON.stringify(record) + \"\\n\",\n    },\n  },\n];\n"
      }
    },
    {
      "id": "c1000000-0000-4000-8000-00000000000e",
      "name": "Incident Found?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        464,
        288
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 1
          },
          "conditions": [
            {
              "id": "c1000000-0000-4000-8000-00000000000e",
              "leftValue": "={{ $json.found }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      }
    },
    {
      "id": "c1000000-0000-4000-8000-00000000000f",
      "name": "Respond Unknown Incident",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        688,
        464
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({status: \"rejected\", errors: [\"no incident \" + $json.incident_id + \" in the store, so no transition was recorded\"]}) }}",
        "options": {
          "responseCode": 404
        }
      }
    },
    {
      "id": "c1000000-0000-4000-8000-000000000010",
      "name": "Transition Allowed?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        688,
        224
      ],
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict",
            "version": 1
          },
          "conditions": [
            {
              "id": "c1000000-0000-4000-8000-000000000010",
              "leftValue": "={{ $json.allowed }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      }
    },
    {
      "id": "c1000000-0000-4000-8000-000000000011",
      "name": "Respond Illegal Transition",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        912,
        400
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({status: \"rejected\", errors: [$json.reason], incident_id: $json.incident_id, current_status: $json.current_status, requested_status: $json.requested_status, allowed_next: $json.allowed_next}) }}",
        "options": {
          "responseCode": 409
        }
      }
    },
    {
      "id": "c1000000-0000-4000-8000-000000000012",
      "name": "Build Transition Line",
      "type": "n8n-nodes-base.convertToFile",
      "typeVersion": 1.1,
      "position": [
        912,
        160
      ],
      "parameters": {
        "operation": "toText",
        "sourceProperty": "transition_jsonl",
        "options": {}
      }
    },
    {
      "id": "c1000000-0000-4000-8000-000000000013",
      "name": "Append Transition Record",
      "type": "n8n-nodes-base.readWriteFile",
      "typeVersion": 1.1,
      "position": [
        1136,
        160
      ],
      "parameters": {
        "operation": "write",
        "fileName": "/data/arkon/incident_transitions.jsonl",
        "options": {
          "append": true
        }
      }
    },
    {
      "id": "c1000000-0000-4000-8000-000000000014",
      "name": "Respond Recorded",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1.1,
      "position": [
        1360,
        160
      ],
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({status: \"transition_recorded\", transition_id: $('Build Transition Record').item.json.transition.transition_id, incident_id: $('Build Transition Record').item.json.transition.incident_id, from_status: $('Build Transition Record').item.json.transition.from_status, to_status: $('Build Transition Record').item.json.transition.to_status, recorded_at: $('Build Transition Record').item.json.transition.recorded_at, minutes_since_created: $('Build Transition Record').item.json.transition.minutes_since_created, acknowledged_within_window: $('Build Transition Record').item.json.transition.acknowledged_within_window}) }}",
        "options": {}
      }
    },
    {
      "id": "e1000000-0000-4000-8000-000000000052",
      "name": "Sync Store",
      "type": "n8n-nodes-base.executeWorkflow",
      "typeVersion": 1.3,
      "position": [
        1360,
        -16
      ],
      "onError": "continueRegularOutput",
      "parameters": {
        "workflowId": {
          "__rl": true,
          "mode": "id",
          "value": "arkonStoreSync1"
        },
        "workflowInputs": {
          "mappingMode": "defineBelow",
          "value": {
            "source": "transition",
            "rebuild": false
          },
          "matchingColumns": [],
          "schema": [
            {
              "id": "source",
              "displayName": "source",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "string",
              "canBeUsedToMatch": true,
              "removed": false
            },
            {
              "id": "rebuild",
              "displayName": "rebuild",
              "required": false,
              "defaultMatch": false,
              "display": true,
              "type": "boolean",
              "canBeUsedToMatch": true,
              "removed": false
            }
          ],
          "attemptToConvertTypes": false,
          "convertFieldsToString": false
        },
        "mode": "once",
        "options": {
          "waitForSubWorkflow": false
        }
      }
    }
  ],
  "connections": {
    "Transition Intake": {
      "main": [
        [
          {
            "node": "Validate Transition",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validate Transition": {
      "main": [
        [
          {
            "node": "Valid?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Valid?": {
      "main": [
        [
          {
            "node": "Simulated Failure?",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Respond Invalid",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Simulated Failure?": {
      "main": [
        [
          {
            "node": "Respond Simulated Unavailable",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Read Incident Store",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read Incident Store": {
      "main": [
        [
          {
            "node": "Extract Store Text",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Respond Store Unavailable",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Extract Store Text": {
      "main": [
        [
          {
            "node": "Read Transition Store",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Read Transition Store": {
      "main": [
        [
          {
            "node": "Extract Transitions Text",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Respond Transition Log Unavailable",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Extract Transitions Text": {
      "main": [
        [
          {
            "node": "Build Transition Record",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Transition Record": {
      "main": [
        [
          {
            "node": "Incident Found?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Incident Found?": {
      "main": [
        [
          {
            "node": "Transition Allowed?",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Respond Unknown Incident",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Transition Allowed?": {
      "main": [
        [
          {
            "node": "Build Transition Line",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Respond Illegal Transition",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build Transition Line": {
      "main": [
        [
          {
            "node": "Append Transition Record",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Append Transition Record": {
      "main": [
        [
          {
            "node": "Respond Recorded",
            "type": "main",
            "index": 0
          },
          {
            "node": "Sync Store",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1",
    "binaryMode": "separate",
    "availableInMCP": false
  },
  "pinData": {}
}
